#!/bin/bash

app_dir=`pwd`
prj_file="$app_dir/project.json"

path=`jq -r ".path" "$prj_file"`
repo=`jq -r ".docker.repo" "$prj_file"`
project=`jq -r ".docker.name" "$prj_file"`

docker run -ti --rm  -v $app_dir:/code -p 4001:8080 $repo/$project:latest "$@"
